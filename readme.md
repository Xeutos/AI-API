# README

## Setup

To use the API, create a `.env` file in the root directory and add your OpenRouter API key in the following format:

```env
API_KEY=YourKeyHere
```

## API Endpoint

```
To call the AI model, use the following endpoint:

/api/v1/chat
```
